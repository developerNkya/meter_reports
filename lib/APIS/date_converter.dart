
//function to handle date conveter

date_converter(String someDate){

  // var dateWithoutTime = some_date.substring(0, 10);
  //
  // var inputTime = some_date.substring(21);
  // var inputFormat = DateFormat("h:mm a");
  // var outputFormat = DateFormat("HH:mm.ss");
  // var formattedTime = outputFormat.format(inputFormat.parse(inputTime));
  //
  // var combinedDateTime = '$dateWithoutTime $formattedTime';
  // return combinedDateTime;

  return someDate;

}
